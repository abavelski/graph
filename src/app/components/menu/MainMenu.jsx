import React from 'react';
import styles from './styles';

const MainMenu = () => (
    <header>
        <div style={styles.div}><span style={styles.txt}>GRAPH</span></div>
    </header>);

export default MainMenu;
